
# План курса JavaScript (16 встреч)

## 1. Введение в JavaScript

- Где используется JavaScript
- Как запускать JS в браузере
- Подключение скрипта к HTML
- Консоль и DevTools
- Базовый синтаксис

## 2. Условия и логика

- `boolean`
- Операторы сравнения
- `truthy / falsy`
- `if / else`
- Тернарный оператор
- Проверки ввода из `prompt()`
- `switch`

## 3. Циклы

- `while`
- `do...while`
- `for`
- `break / continue`
- Типовые паттерны (сумма, поиск, валидация ввода)

## 4. Функции (база)

- Виды функций: `Function Declaration`, `Function Expression`, `Arrow Function`
- Параметры и аргументы
- `return` и ранний `return`
- Область видимости (scope)
- Default args (параметры по умолчанию)
- Rest parameters `...args`
- Функции как значения: callback
- Замыкания (closure) — базовое понимание

## 5. Массивы: база + методы + обработка данных (объединённая лекция)

- Создание массива, индексы, `length`
- Перебор: `for`, `for...of`
- Базовые методы: `push/pop`, `shift/unshift`, `slice/splice`
- Методы обработки: `map`, `filter`, `find`, `reduce` (в простом виде)
- Колбэки в методах массива (как практическое применение функций)
- Типовые паттерны: найти / отфильтровать / преобразовать / посчитать

## 6. Объекты

- Свойства и методы объекта
- `this` (база)
- Перебор: `for...in`, `Object.keys/values/entries`
- Копирование объектов (поверхностное)

## 7. ООП

- Прототипы (кратко, как база)
- Классы: `class`, `constructor`, методы
- Наследование: `extends`, `super`

## 8. DOM

- Структура документа
- Поиск элементов: `querySelector`, `querySelectorAll`
- Изменение содержимого: `textContent`, `innerHTML`
- Атрибуты и классы
- Создание и добавление элементов

## 9. События и формы

- Обработчики событий
- Объект события `event`
- Делегирование (база)
- Формы: чтение данных, отправка
- Валидация, `preventDefault()`

## 10. BOM и Storage

- `location`, `history` (минимально нужное)
- Таймеры: `setTimeout`, `setInterval`
- `localStorage`, `sessionStorage`

## 11. Асинхронность и API

- Callback → Promise → `async/await`
- Обработка ошибок
- `fetch`
- JSON
- Базовая работа с API

## 12. Модули

- `import / export`
- Структура проекта
- Разделение кода по файлам

## 13. Tooling

- npm
- Vite
- Структура проекта
- Сборка
- Подключение библиотек

## 14. Deploy / production / оптимизация

- Сборка проекта
- Окружения (базово)
- Оптимизация (минимально)
- Деплой (например, GitHub Pages / Netlify)

## 15. Тестирование

## 16. Финальная встреча
